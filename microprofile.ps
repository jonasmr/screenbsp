
uniform sampler2D tex; 

varying vec3 TC;

void main(void)  
{  
	vec4 color = texture(tex, TC.xy);
	color = TC.y > 1.5 ? vec4(1,1,1,color.w) : color;
	gl_FragColor.xyz = gl_Color.xyz;
	gl_FragColor.w = 1;
//	gl_FragColor.x += 0.5;
}
